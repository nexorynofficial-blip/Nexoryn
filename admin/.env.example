# Base URL of the backend API (see ../backend). No trailing slash.
VITE_API_BASE_URL=http://localhost:3001

# Base URL of the AI project-assistant widget's Python backend (see
# FloatingAgentWidget.tsx / WIDGET_README.md). No trailing slash.
VITE_NEXORYN_AGENT_URL=http://localhost:8000
